
task :default => :present

desc "Run all the example tests within the exercise"
task :spec do
  system "rspec spec"
end

desc "Start the presentation"
task :present do
  system "showoff serve presentation"
end